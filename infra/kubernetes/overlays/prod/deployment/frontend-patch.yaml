apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  template:
    spec:
      containers:
      - name: frontend
        env:
        - name: API_PROTOCOL
          value: "http"
        - name: API_URL
          value: "gouguel.com:8080"
